apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: zigbee2mqtt
  namespace: smarthub
spec:
  chart:
    spec:
      chart: zigbee2mqtt
      sourceRef:
        kind: HelmRepository
        name: koenkk
        namespace: flux-system
      version: 0.1.0
  interval: 5m
  valuesFrom:
  - kind: ConfigMap
    name: zigbee2mqtt-values
